#include<stdio.h>
int main(){
	int a=2,b=10,c=30;
	float avg;	
	avg=a*b*c/100; //We used the wrong formula for average thats why logical error occured
	printf("The Average of Three Number is : %f ",avg);
	return 0;
}
